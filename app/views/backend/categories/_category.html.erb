  <tr>
  <td><%= category.name %></td>
   
       
    <td class='text-right'>
     <button type="button" class="btn btn-secondary"><%= link_to 'Editar', edit_backend_category_path(category), class: "badge badge-secondary" %></button>
     <button type="button" class="btn btn-secondary"><%= link_to 'Borrar', backend_category_path(category), method: :delete, data: { confirm: 'Esta seguro?' }, class: "badge badge-secondary" %></button>
     </td>
</tr>