
  <tr>
    <td><%= product.title %></td>
    <td><%= product.active %></td>
    <td><%= product.price %></td>

      <td class='text-right'>

      <button type="button" class="btn btn-secondary"><%= link_to 'Ver', backend_product_path(product), class: "badge badge-secondary" %></button>
      <button type="button" class="btn btn-secondary"><%= link_to 'Editar', edit_backend_product_path(product), class: "badge badge-secondary" %></button>
      <button type="button" class="btn btn-secondary"><%= link_to 'Borrar', backend_product_path(product), method: :delete, data: { confirm: 'Esta seguro?' }, class: "badge badge-secondary"%></button>
      </td>
   </tr>


