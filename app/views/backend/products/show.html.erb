<h1>Publicacion #<%= @product.id %></h1>

<div class='card'>
  <div class='card-body'>
    <div class="mb-3">
      <b>Título</b><br/>
      <%= @product.title %>
    </div>
    <div class="mb-3">
      <b>Activo</b><br/>
      <%= @product.active %>
    </div>
    <div class="mb-3">
      <b>Precio</b><br/>
      <%= @product.price %>
    </div>
  </div>

  <div class='card-footer'>
    <%= link_to 'Volver', backend_products_path, class: 'btn btn-outline-secondary' %>
  </div>
</div>